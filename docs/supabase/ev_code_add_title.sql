-- ============================================================================
-- ev_code 테이블에 title 컬럼 추가
-- ============================================================================
-- 목적: ev_code 테이블에 제목(title) 컬럼 추가
-- ============================================================================

-- title 컬럼 추가
ALTER TABLE public.ev_code 
ADD COLUMN IF NOT EXISTS title VARCHAR(255);

-- 컬럼 코멘트 추가
COMMENT ON COLUMN public.ev_code.title IS '제목 - 코드에 대한 제목 또는 이름';
